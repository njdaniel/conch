// Command conch is the human client: a Bubble Tea TUI plus a plain scriptable
// CLI mode (conch send, conch approvals list, conch approve). It is a client
// of conchd's public REST/WS API. See ROADMAP.md; implementation begins in P0.
package main

import "fmt"

var version = "v0.0.0-dev"

func main() {
	fmt.Printf("conch %s — not implemented yet; see ROADMAP.md\n", version)
}
