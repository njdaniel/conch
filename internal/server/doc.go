// Package server implements conchd's core: the SQLite storage layer (WAL
// mode), the WebSocket hub, the REST API, the approval-object state machine,
// and the MCP endpoint for agents. All wire shapes come from pkg/schema.
package server
