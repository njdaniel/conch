// Package cli implements the conch client: the Bubble Tea TUI and the plain
// scriptable CLI mode. It talks only to conchd's public REST/WS API — anything
// it can do exists in the API first (golden rule 4, ADR-001).
package cli
