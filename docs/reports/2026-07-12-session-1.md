# Session 1 report — bootstrap

- **Date:** 2026-07-12
- **Session:** Founding bootstrap per the handoff document; no feature code written.
- **Branch/PR:** `session-1/bootstrap`, closes #1.

## What was created

**Scaffolding:** Go module `github.com/njdaniel/conch` (go 1.24, zero dependencies yet), placeholder `cmd/conchd` + `cmd/conch` mains, package docs for `pkg/schema` / `internal/server` / `internal/cli`, AGPL-3.0 LICENSE, `.gitignore`, Makefile (`build/test/lint/vet/fmt/check/schema-compat/depgate/hooks-install`), full README (pitch, name origin, non-goals, status).

**Governance:** `CLAUDE.md` (7 golden rules + working agreements); six agents in `.claude/agents/` (protocol-designer *fable*, server-engineer *sonnet*, cli-engineer *sonnet*, reviewer *fable* read-only, docs-agent *sonnet*, explorer *haiku* read-only); git hooks in `.githooks/` (pre-commit: gofmt, vet, golangci-lint, test, schema-compat, depgate — install via `make hooks-install`); `scripts/schema-compat.sh` (golden-fixture immutability gate, no-ops until fixtures exist) and `scripts/depgate.sh` + `deps-allowlist.txt` (seeded: modernc.org/sqlite, bubbletea, lipgloss); four skills (`schema-change`, `dogfood-check`, `test-and-fix`, `principal-review`); issue templates (task, ADR proposal) and PR template with invariant checklist + approval-path flag.

**Docs:** ADR-000 (charter, D1–D15, authority table), ADR-001 (MCP-native + parity), ADR-002 (single-binary + SQLite), `docs/design/approval-object.md` (D9 in full: entity, state machine pending→escalated→resolved/expired, decisions/resolution event, blocking vs polling semantics, ntfy behavior, audit chain), ROADMAP.md (§7 with kill/success criteria verbatim).

**Backlog:** milestones `P0 Spike` / `P1 Core Loop` / `P2 Hardening` / `P3 Icebox`; labels (`area/*`, `approval-path`, `blocked`, `adr-needed`); issues #1–#27 — P0 (#2–#8) and P1 (#9–#19) written to the template standard with blocked-by ordering, P2/P3 (#20–#27) as stubs per §5.

**CI:** `.github/workflows/ci.yml` (gofmt, vet, golangci-lint v2, `go test -race`, schema-compat, depgate) + `.golangci.yml`. Verified green locally via `make check`.

## Decisions taken on delegated open items (§9)

| Item | Decision |
|---|---|
| Makefile vs task runner | Makefile — zero extra tooling, matches single-binary ethos |
| Version scheme | SemVer pre-1.0 (`v0.x.y`), tags only at Nick-gated releases |
| Changelog | Keep a Changelog format; `CHANGELOG.md` starts at first release |
| Hook mechanism | Committed git hooks (`.githooks/` + `core.hooksPath`); CI is the identical remote layer |
| Deps allowlist seed | sqlite/bubbletea/lipgloss pre-approved from D2/D5; WS lib (#4) and MCP SDK (#11) explicitly need sign-off |

## Deviations from the handoff

1. **Default branch renamed `master` → `main`** — the handoff assumes branch protection on `main`; the repo was created with `master`. Judged mechanical, not a locked decision.
2. **LICENSE replaced GPL-3.0 → AGPL-3.0** — the repo was created with GPL-3.0; D13 locks AGPL-3.0. Correction toward the locked decision, not a deviation from it.

No proposed ADRs — nothing in the handoff proved wrong or underspecified during bootstrap.

## Implementation order (ready for cold sessions)

P0: **#2** storage → **#3** conchd skeleton → **#4** WS hub ∥ **#5** REST → **#6** bare CLI → **#7** demo + spike report (kill-criterion verdict goes to Nick) → **#8** schema-compat activation.
P1 (after #7 verdict = proceed): **#9** schema freeze → then #10, and #11/#15/#17 in parallel → #12 → #13/#14/#16/#18 → **#19** dogfood E2E.

## Nick's action items

1. Review and merge the bootstrap PR (it contains ADR-000/001/002 — merging is the sign-off).
2. Flip branch protection on `main`: require the `check` CI job + 1 approving review; disallow force pushes. (Settings → Branches → Add rule.)
3. Confirm the `deps-allowlist.txt` seed (sqlite, bubbletea, lipgloss).
4. Pending sign-offs queued in the backlog: WebSocket library (#4), MCP SDK choice (#11).
